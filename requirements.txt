# Rangos: mínimo probado y techo en la siguiente versión mayor.
# Dependabot propone un PR cuando sale una versión fuera del rango (p. ej. Flask 4).
Flask>=3.0,<4
openpyxl>=3.1,<4
waitress>=3.0,<4
psycopg[binary]>=3.1,<4
psycopg_pool>=3.2,<4
python-dotenv>=1.0,<2
